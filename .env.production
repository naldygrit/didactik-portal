# Vercel/production build config.
# This portal has no deployed backend yet, so the production build runs on the
# MSW mock catalogue — a self-contained, deterministic DEMO (ideal for the
# MultiChoice pitch link). When a real API is deployed, set this to false (here
# or as a Vercel env var) and point the app at the live backend.
VITE_USE_MOCK=true
